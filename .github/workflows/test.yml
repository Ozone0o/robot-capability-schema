name: test

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]" build
      - name: Run tests
        run: |
          pytest
      - name: Lint Axiom packages
        run: ruff check src tests examples
      - name: Validate example contract
        run: axiom validate examples/robot.yaml
      - name: Build and smoke-test wheel
        run: |
          python -m build
          python -m venv /tmp/axiom-wheel-smoke
          /tmp/axiom-wheel-smoke/bin/pip install dist/axiom-*.whl
          /tmp/axiom-wheel-smoke/bin/axiom --help
